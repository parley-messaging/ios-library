public enum ParleyPosition {
    case top
    case bottom
    case left
    case right
}

public enum ParleyPositionVertical: Sendable {
    case top
    case bottom
}

public enum ParleyPositionHorizontal {
    case left
    case right
}
