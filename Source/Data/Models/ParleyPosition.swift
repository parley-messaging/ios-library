public enum ParleyPosition {
    case top
    case bottom
    case left
    case right
}

public enum ParleyPositionVertical {
    case top
    case bottom
}

public enum ParleyPositionHorizontal {
    case left
    case right
}
